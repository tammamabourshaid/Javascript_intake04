const image = document.createElement("img");// created DOM element
const paragraph = document.createElement("p");
document.body.appendChild(image);//added element to HTML file via script
document.body.appendChild(paragraph);