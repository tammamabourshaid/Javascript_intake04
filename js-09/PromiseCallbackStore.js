class promiseCallbackStore {

    constructor(store) {
        this.store=store;
        this.groceries = []; 
        this.total=0
    }

    put(item, amount) {
        let times = 0
        while (times <= amount)
        const Object = this.store()
            this.groceries.push(item);
        times ++
        }
    
        

        
    getTotal(){
        return this.total;
    }
    
    
}
module.export={promiseCallbackStore};