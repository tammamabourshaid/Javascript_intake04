class BankAccount {
    deposit(amount) {
        return this.amount;

    }
    withdraw(amount) {
        return this.amount;

    }
    getBalance(BankAccount){
        if (deposit === 0) {
            return 0;
        } else if (deposit > 0) {
            balance = deposit - withdraw;
        }
        return balance;
    }
}