function pipe(){

            
        const formula = (accumulator, currentValue) =>
            (accumulator (currentValue ));0
    
        const result = value.reduce(formula, 0);
        console.log(result);
    
        return result;
    



}
module.exports={pipe};