function pipe([array]) {
    if (array.length < 1 || array == undefined) {
        return 1;
    }
    let product = 0;
    let first = array[0];
    while (i = 1, i < array.length - 1, i++) {
        return first *= array[i]
    }
}

module.exports = { pipe };