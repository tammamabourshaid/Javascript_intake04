

function toPairs (city) {

    let population = ["population", city.population];
    let size = ["size", city.size];
    let pairs = [population, size];
  
    return pairs;
  };
  
  module.exports = {toPairs};


  










module.exports = {toPairs};