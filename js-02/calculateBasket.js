const calculateBasket = array => {
    array.forEach(product => {
        let price,
            total = (price * amount);
        console.log(total);
    });
};

module.exports = { calculateBasket };